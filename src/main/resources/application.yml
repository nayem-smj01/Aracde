arcadedb:
  host: localhost
  port: 2480
  username: root
  password: 2cb866ef
  connection:
    timeout: 5000
    pool:
      max-size: 10

server:
  port: 8086

logging:
  level:
    com.example: DEBUG
    com.arcadedb: INFO

# Swagger/OpenAPI Configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operations-sorter: method
    tags-sorter: alpha
